plugins {
    id 'java'
    id 'application'
}
if (hasProperty('buildScan')) {
    buildScan {
        termsOfServiceUrl = 'https://gradle.com/terms-of-service'
        termsOfServiceAgree = 'yes'
    }
}
run {
    standardInput = System.in
}
group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.2'
    implementation project(':logic')
    implementation project(':display')
    implementation project(':simulation')
}

test {
    useJUnitPlatform()
}
application {
<<<<<<< HEAD
    mainClass = 'pl.pwr.ludoSimulator.application.Application'
}

jar {
    from {
        (configurations.runtimeClasspath).collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
    manifest {
        attributes 'Main-Class': 'pl.pwr.ludo.simulator.application.Application'
    }
}
=======
    mainClass = 'pl.pwr.ludo.simulator.application.Application'
}
>>>>>>> 69cdd820c5323c43aab81e2dfe98735c829551e1
